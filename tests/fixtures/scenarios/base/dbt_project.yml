name: 'jaffle_shop'

config-version: 2
version: '0.1'

profile: 'jaffle_shop'

model-paths: ["models"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets:
    - "target"
    - "logs"

require-dbt-version: [">=1.11.0", "<2.0.0"]

models:
  jaffle_shop:
    +materialized: table
    staging:
      +materialized: view
